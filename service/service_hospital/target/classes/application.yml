server:
  port: 8201
spring:
  application:
    name: service-hospital
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/yygh_hosp?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
  # 返回json的全局时间格式
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
# mybatis配置
#mybatis-plus:
#  mapper-locations: classpath:com/xxxx/intelligence/hospital/mapper/xml/*.xml
